<div class="container my-4">
  <h2 class="mb-4 text-center">Your Bookings</h2>

  <!-- Past Games -->
  {{#if pastGameBookings.length}}
  <h4 class="mb-3">Past Games</h4>
  <div class="row g-4">
    {{#each pastGameBookings}}
    <div class="col-md-6 col-lg-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body">
          <h5 class="card-title">{{title}}</h5>
          <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
          <p class="card-text"><strong>Location:</strong> {{location}}</p>

          {{#ifCond players.length '>' 1}}
          <button class="btn btn-primary btn-sm mb-2 btn-rate" data-bs-toggle="modal" data-bs-target="#rateModal"
            data-booking-id="{{_id}}" data-players='{{json players}}' data-host='{{host}}'>
            Rate Players
          </button>
          {{/ifCond}}

          {{!-- <div id="rate-players-{{_id}}" class="rate-players-panel d-none">
            <form class="rate-form" data-booking-id="{{_id}}">
              {{#each players}}
              {{#ifCond this._id '!=' ../host}}
              <div class="rating-section mb-2">
                <a href="/profile/userview/{{_id}}"
                  class="d-block mb-1 fw-semibold text-decoration-none text-dark">{{username}}</a>
                <div class="stars {{#if hasBeenRated}}disabled{{/if}}" data-user-id="{{_id}}"
                  data-rated="{{hasBeenRated}}">
                  {{#each (range 1 5)}}
                  <i class="bi bi-star-fill star {{#if (lte this ../ratedScore)}}selected{{/if}}"
                    data-score="{{this}}"></i>
                  {{/each}}
                </div>

                <!-- Review input field -->
                <textarea class="form-control mt-2 review-input" name="review_{{_id}}" rows="2"
                  placeholder="Write a short review...">{{#if hasBeenRated}}{{ratedReview}}{{/if}}</textarea>

                {{#if hasBeenRated}}
                <input type="hidden" name="ratings[]" value='{{json (object "userId" _id "score" ratedScore)}}' />
                {{/if}}
              </div>
              {{/ifCond}}
              {{/each}}
              <div class="container my-4">
                <h2 class="mb-4 text-center">Your Bookings</h2>

                <!-- Past Games -->
                {{#if pastGameBookings.length}}
                <h4 class="mb-3">Past Games</h4>
                <div class="row g-4">
                  {{#each pastGameBookings}}
                  <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                      <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
                        <p class="card-text"><strong>Location:</strong> {{location}}</p>

                        {{#ifCond players.length '>' 1}}
                        <button class="btn btn-primary btn-sm mb-2 rate-players-btn" data-booking-id="{{_id}}">
                          Rate Players
                        </button>
                        {{/ifCond}}

                        <div id="rate-players-{{_id}}" class="rate-players-panel d-none">
                          <form class="rate-form" data-booking-id="{{_id}}">
                            {{#each players}}
                            {{#ifCond this._id '!=' ../host}}
                            <div class="rating-section mb-2">
                              <a href="/profile/userview/{{_id}}"
                                class="d-block mb-1 fw-semibold text-decoration-none text-dark">{{username}}</a>
                              <div class="stars {{#if hasBeenRated}}disabled{{/if}}" data-user-id="{{_id}}"
                                data-rated="{{hasBeenRated}}">
                                {{#each (range 1 5)}}
                                <i class="bi bi-star-fill star {{#if (lte this ../ratedScore)}}selected{{/if}}"
                                  data-score="{{this}}"></i>
                                {{/each}}
                              </div>

                              <!-- Review input field -->
                              <textarea class="form-control mt-2 review-input" name="review_{{_id}}" rows="2"
                                placeholder="Write a short review...">{{#if hasBeenRated}}{{ratedReview}}{{/if}}</textarea>

                              {{#if hasBeenRated}}
                              <input type="hidden" name="ratings[]"
                                value='{{json (object "userId" _id "score" ratedScore)}}' />
                              {{/if}}
                            </div>
                            {{/ifCond}}
                            {{/each}}
                            <button class="btn btn-primary btn-sm mb-2 btn-rate" data-bs-toggle="modal"
                              data-bs-target="#rateModal" data-booking-id="{{_id}}" data-players='{{json players}}'>
                              Rate Players
                            </button>
                          </form>
                        </div>
                      </div>
                    </div>
                  </div>
                  {{/each}}
                </div>
                {{else}}
                <h4 class="mb-3">Past Games</h4>
                <p class="text-muted text-center">You have no past game bookings yet.</p>
                {{/if}}

                <hr>
                <!-- Past Gym Sessions -->
                {{#if pastGymBookings.length}}
                <h4 class="mt-5 mb-3">Past Gym Sessions</h4>
                <div class="row g-4">
                  {{#each pastGymBookings}}
                  <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                      <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
                        <p class="card-text"><strong>Gym:</strong> {{gym}}</p>

                        {{#ifCond players.length '>' 1}}
                        <button class="btn btn-primary btn-sm mb-2 rate-players-btn" data-booking-id="{{_id}}">
                          Rate Players
                        </button>
                        {{/ifCond}}

                        <div id="rate-players-{{_id}}" class="rate-players-panel d-none">
                          <form class="rate-form" data-booking-id="{{_id}}">
                            {{#each players}}
                            {{#ifCond this._id '!=' ../host}}
                            <div class="rating-section mb-2">
                              <a href="/profile/userview/{{_id}}"
                                class="d-block mb-1 fw-semibold text-decoration-none text-dark">{{username}}</a>
                              <div class="stars {{#if hasBeenRated}}disabled{{/if}}" data-user-id="{{_id}}"
                                data-rated="{{hasBeenRated}}">
                                {{#each (range 1 5)}}
                                <i class="bi bi-star-fill star {{#if (lte this ../ratedScore)}}selected{{/if}}"
                                  data-score="{{this}}"></i>
                                {{/each}}
                              </div>

                              <!-- Review input field -->
                              <textarea class="form-control mt-2 review-input" name="review_{{_id}}" rows="2"
                                placeholder="Write a short review...">{{#if hasBeenRated}}{{ratedReview}}{{/if}}</textarea>

                              {{#if hasBeenRated}}
                              <input type="hidden" name="ratings[]"
                                value='{{json (object "userId" _id "score" ratedScore)}}' />
                              {{/if}}
                            </div>
                            {{/ifCond}}
                            {{/each}}

                            <button class="btn btn-primary btn-sm mb-2 btn-rate" data-bs-toggle="modal"
                              data-bs-target="#rateModal" data-booking-id="{{_id}}" data-players='{{json players}}'>
                              Rate Players
                            </button>
                          </form>
                        </div>
                      </div>
                    </div>
                  </div>
                  {{/each}}
                </div>
                {{else}}
                <h4 class="mt-5 mb-3">Past Gym Sessions</h4>
                <p class="text-muted text-center">You have no past gym sessions yet.</p>
                {{/if}}

                <hr>

                <!-- Future Games and Sessions -->
                {{#if futureGameBookings.length}}
                <h4 class="mt-5 mb-3">Upcoming Games</h4>
                <div class="row g-4">
                  {{#each futureGameBookings}}
                  <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                      <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
                        <p class="card-text"><strong>Location:</strong> {{location}}</p>
                      </div>
                    </div>
                  </div>
                  {{/each}}
                </div>
                {{else}}
                <h4 class="mt-5 mb-3">Upcoming Games</h4>
                <p class="text-muted text-center">No upcoming games booked.</p>
                {{/if}}


                {{#if futureGymBookings.length}}
                <h4 class="mt-5 mb-3">Upcoming Gym Sessions</h4>
                <div class="row g-4">
                  {{#each futureGymBookings}}
                  <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                      <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
                        <p class="card-text"><strong>Gym:</strong> {{gym}}</p>
                      </div>
                    </div>
                  </div>
                  {{/each}}
                </div>
                {{else}}
                <h4 class="mt-5 mb-3">Upcoming Gym Sessions</h4>
                <p class="text-muted text-center">No upcoming gym sessions booked.</p>
                {{/if}}
              </div>

              <!-- Rating & Review Modal -->
              <div class="modal fade" id="rateModal" tabindex="-1" aria-labelledby="rateModalLabel" aria-hidden="true">
                <div class="modal-dialog modal-dialog-scrollable">
                  <div class="modal-content">
                    <form id="rateForm">
                      <div class="modal-header bg-success text-white">
                        <h5 class="modal-title" id="rateModalLabel">Rate and Review Players</h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"
                          aria-label="Close"></button>
                      </div>

                      <div class="modal-body" id="rateFields">
                        <!-- Rating fields will be injected here -->
                      </div>

                      <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"
                          id="rateCancel">Cancel</button>
                        <button type="submit" class="btn btn-success">Submit</button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>



              <script src="/js/bookings.js"></script>
              <!-- Bootstrap Bundle with Popper (for modal functionality) -->
              <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
            </form>
          </div> --}}
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <h4 class="mb-3">Past Games</h4>
  <p class="text-muted text-center">You have no past game bookings yet.</p>
  {{/if}}

  <hr>
  <!-- Past Gym Sessions -->
  {{#if pastGymBookings.length}}
  <h4 class="mt-5 mb-3">Past Gym Sessions</h4>
  <div class="row g-4">
    {{#each pastGymBookings}}
    <div class="col-md-6 col-lg-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body">
          <h5 class="card-title">{{title}}</h5>
          <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
          <p class="card-text"><strong>Gym:</strong> {{gym}}</p>

          {{#ifCond players.length '>' 1}}
          <button class="btn btn-primary btn-sm mb-2 btn-rate" data-bs-toggle="modal" data-bs-target="#rateModal"
            data-booking-id="{{_id}}" data-players='{{json players}}' data-host='{{host}}'>
            Rate Players
          </button>
          {{/ifCond}}

          {{!-- <div id="rate-players-{{_id}}" class="rate-players-panel d-none">
            <form class="rate-form" data-booking-id="{{_id}}">
              {{#each players}}
              {{#ifCond this._id '!=' ../host}}
              <div class="rating-section mb-2">
                <a href="/profile/userview/{{_id}}"
                  class="d-block mb-1 fw-semibold text-decoration-none text-dark">{{username}}</a>
                <div class="stars {{#if hasBeenRated}}disabled{{/if}}" data-user-id="{{_id}}"
                  data-rated="{{hasBeenRated}}">
                  {{#each (range 1 5)}}
                  <i class="bi bi-star-fill star {{#if (lte this ../ratedScore)}}selected{{/if}}"
                    data-score="{{this}}"></i>
                  {{/each}}
                </div>

                <!-- Review input field -->
                <textarea class="form-control mt-2 review-input" name="review_{{_id}}" rows="2"
                  placeholder="Write a short review...">{{#if hasBeenRated}}{{ratedReview}}{{/if}}</textarea>

                {{#if hasBeenRated}}
                <input type="hidden" name="ratings[]" value='{{json (object "userId" _id "score" ratedScore)}}' />
                {{/if}}
              </div>
              {{/ifCond}}
              {{/each}}

              <button class="btn btn-primary btn-sm mb-2 btn-rate" data-bs-toggle="modal" data-bs-target="#rateModal"
                data-booking-id="{{_id}}" data-players='{{json players}}'>
                Rate Players
              </button>
            </form>
          </div> --}}
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <h4 class="mt-5 mb-3">Past Gym Sessions</h4>
  <p class="text-muted text-center">You have no past gym sessions yet.</p>
  {{/if}}

  <hr>

  <!-- Future Games and Sessions -->
  {{#if futureGameBookings.length}}
  <h4 class="mt-5 mb-3">Upcoming Games</h4>
  <div class="row g-4">
    {{#each futureGameBookings}}
    <div class="col-md-6 col-lg-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body">
          <h5 class="card-title">{{title}}</h5>
          <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
          <p class="card-text"><strong>Location:</strong> {{location}}</p>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <h4 class="mt-5 mb-3">Upcoming Games</h4>
  <p class="text-muted text-center">No upcoming games booked.</p>
  {{/if}}


  <hr>

  {{#if futureGymBookings.length}}
  <h4 class="mt-5 mb-3">Upcoming Gym Sessions</h4>
  <div class="row g-4">
    {{#each futureGymBookings}}
    <div class="col-md-6 col-lg-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body">
          <h5 class="card-title">{{title}}</h5>
          <p class="card-text"><strong>Date:</strong> {{formattedDateTime}}</p>
          <p class="card-text"><strong>Gym:</strong> {{gym}}</p>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <h4 class="mt-5 mb-3">Upcoming Gym Sessions</h4>
  <p class="text-muted text-center">No upcoming gym sessions booked.</p>
  {{/if}}
</div>

<!-- Rating & Review Modal -->
<div class="modal fade" id="rateModal" tabindex="-1" aria-labelledby="rateModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-scrollable">
    <div class="modal-content">
      <form id="rateForm">
        <div class="modal-header bg-success text-white">
          <h5 class="modal-title" id="rateModalLabel">Rate and Review Players</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>

        <div class="modal-body" id="rateFields">
          <!-- Rating fields will be injected here -->
        </div>

        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="rateCancel">Cancel</button>
          <button type="submit" class="btn btn-success">Submit</button>
        </div>
      </form>
    </div>
  </div>
</div>



<script src="/js/bookings.js"></script>
<!-- Bootstrap Bundle with Popper (for modal functionality) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>